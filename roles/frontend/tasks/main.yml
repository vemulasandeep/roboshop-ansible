- name: Hello from Frontend
  tasks:
    -name: Print from frontend
     ansible.buitlin.debug:
       msg: Print from frontend
     -name: Print frontend hostname
      ansible.builtin.debug:
        msg: Hostname ={{ansible_hostname}}


